
#define SIZE 20
#define iseven(n)  n % 2 == 0
#define max(a,b) a > b ? a : b

void main()
{
   int a[SIZE];
   int i, num, big, n1, n2;


      for(i=0; i < SIZE; i ++)
             ;

      if (iseven(num))   //  if(a % 2 == 0)
          ;

      big = max(n1,n2);  // n1 > n2 ? n1 : n2

}
